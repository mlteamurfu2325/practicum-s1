name: Playwright Tests
on:
  workflow_dispatch:

jobs:
  playwright:
    name: 'Playwright Tests'
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright/python:v1.42.0-jammy
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright pytest-playwright faster-whisper streamlit pytube openai pysubs2 streamlit_ext streamlit_extras
      - name: Create models directory
        run: |
          mkdir -p models/faster-whisper/
      - name: Download models
        run: |
          python3 deploy/download_faster_whisper_models.py
      - name: Run Streamlit App
        run: |
          cd src/
          streamlit run run_app.py &
      - name: Run your tests
        run: pytest browser_test.py
        env:
          HOME: /root
